# Python 3.11 x64 / Windows + Linux / NVIDIA CUDA 12.8
# Windows portable runtime (install_local.bat) is Python 3.11.9.
#
# Install in two stages from the project root. Torch and Torchaudio are NOT
# listed below, so the second command cannot replace the verified cu128 pair.
#
# Stage 1 - install the matching CUDA 12.8 pair (Torch must stay below 2.8):
#   python -m pip install torch==2.7.1+cu128 torchaudio==2.7.1+cu128 --index-url https://mirrors.nju.edu.cn/pytorch/whl/cu128 --extra-index-url https://mirrors.pku.edu.cn/pypi/simple
#
# Stage 2 - install all remaining project dependencies:
#   python -m pip install -r requirments_cu128_py311.txt
# Windows portable runtime users may replace `python` with `runtime\python.exe -I`.

--index-url https://mirrors.pku.edu.cn/pypi/simple

setuptools>=75.0
wheel>=0.45,<1
packaging>=24.0

faiss-cpu>=1.13.0,<2
librosa>=0.10.2,<0.11
numpy>=1.26.4,<2

# ONNX Runtime 1.19.x uses the CUDA 12 / cuDNN 9 provider ABI. Later current
# releases have moved to CUDA 13, so this compatibility window is intentional.
onnxruntime-gpu>=1.19.2,<1.20
onnx>=1.16.0,<2
coloredlogs>=15.0,<16

praat-parselmouth>=0.4.5,<1
PySide6>=6.5
PySide6-Fluent-Widgets>=1.7.0
PyYAML>=6.0.1
scipy>=1.13.1,<2
sounddevice>=0.5.0,<1
soundfile>=0.13.0,<1
torchfcpe>=0.0.4,<0.1
tqdm>=4.67.0,<5
transformers>=4.49.0,<4.50
websocket-client>=1.6
websockets>=10.4,<11
